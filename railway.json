{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build --workspace=@deaddrop/web && npm run build --workspace=@deaddrop/api"
  },
  "deploy": {
    "startCommand": "npm run start --workspace=@deaddrop/api",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
